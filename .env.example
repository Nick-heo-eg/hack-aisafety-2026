# Copy to .env (NEVER commit .env — see .gitignore).
# Real values live outside the repo at ~/.env on the dev machine.

# Google AI Studio (Gemini). Hackathon-distributed key, share-aware.
# Rotate _2.._4 if quota / rate-limited.
GEMINI_API_KEY=
GEMINI_API_KEY_2=
GEMINI_API_KEY_3=
GEMINI_API_KEY_4=
